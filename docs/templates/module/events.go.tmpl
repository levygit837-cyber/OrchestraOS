package {{MODULE_NAME}}

// EventTypeForStatus returns the event type string for a given status.
func EventTypeForStatus(status Status) string {
	if status == StatusCreated { // TODO: adjust if needed
		return "{{MODULE_NAME}}.created"
	}
	return "{{MODULE_NAME}}." + string(status)
}
