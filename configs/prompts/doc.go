// Package prompts contains the prompt fragment catalog used by the prompt module.
// This package is separate so the catalog can live under configs/ while still
// being embeddable into the binary.
package prompts
