package {{MODULE_NAME}}

const (
	QueryInsert = `
		INSERT INTO {{MODULE_NAME}}s (id, status, created_at, updated_at)
		VALUES ($1, $2, $3, $4)
		RETURNING id`

	QueryGetByID = `
		SELECT id, status, created_at, updated_at
		FROM {{MODULE_NAME}}s WHERE id = $1`

	QueryList = `
		SELECT id, status, created_at, updated_at
		FROM {{MODULE_NAME}}s ORDER BY created_at DESC`
)
