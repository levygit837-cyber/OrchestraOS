// Package transition provides pure, module-free types and helpers for state-machine
// transitions and event operations. It may be imported by any package in the
// project, including internal/modules/*, but must never import modules itself.
package transition
