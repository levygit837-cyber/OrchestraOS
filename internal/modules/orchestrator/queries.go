package orchestrator

// Queries for the orchestrator module.
// Currently a placeholder; the OrchestratorService delegates all persistence
// to injected domain services. Future versions may add orchestrator-specific
// tables (e.g., orchestrator_state, execution_plans).
